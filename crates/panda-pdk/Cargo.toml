[package]
name = "panda-pdk"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "Guest-side Panda Plugin Development Kit (ABI constants and host-call helpers)"

[lib]
name = "panda_pdk"
path = "src/lib.rs"

[dependencies]
