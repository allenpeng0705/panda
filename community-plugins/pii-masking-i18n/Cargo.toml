[package]
name = "wasm-plugin-pii-masking-i18n"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Community Wasm: locale-aware PII-style masking for common UTF-8 patterns"

[lib]
crate-type = ["cdylib"]

[dependencies]
panda-pdk = { path = "../../crates/panda-pdk" }
