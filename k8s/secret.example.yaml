apiVersion: v1
kind: Secret
metadata:
  name: panda-secrets
  labels:
    app.kubernetes.io/name: panda
type: Opaque
stringData:
  # Optional: protects /metrics, /plugins/status, /tpm/status, /mcp/status.
  admin-status-secret: "replace-me"
  # Optional for shared TPM across replicas.
  # redis-url: "redis://redis.default.svc.cluster.local:6379"
